//
//  UserProfileView.swift
//  Shat-App
//
//  Created by Neil Sano on 2020-11-15.
//

import SwiftUI
/*
struct UserProfileView: View {
    @EnvironmentObject var session : CurrentSessionViewModel
    //    let userInfo = session.currentUser?.providerData[indexPath.row]
    //    let gradient = Gradient(colors: [.gray,.purple])
    var body: some View {
        VStack{
            
            Form{
                Section(header: Text("User Info")){
                    Text("Name: \(session.getUserName() ?? "No User")") // plan to use the user's actual name
                    Text("Email: \(session.getUserEmail() ?? "No Email")")
                }
                //Eventually Have an Image that will use the Camera or Photo Library
                
                Section(header: Text("User Options")){
                    Button(action: {
                        print("Edit User Pressed")
                    }, label: {
                        Text("Edit User")
                    })
                    Button(action:{
                        print("Delete User Pressed")
                        //todo: send out an alert asking if the user's surely wants to delete account
                    }, label:{
                        Text("Delete User").foregroundColor(.red)
                    })
                }
            }
            
        }
        .navigationTitle("Your Profile")
    }
}

struct UserProfileView_Previews: PreviewProvider {
    static var previews: some View {
        UserProfileView()
    }
}
 */
